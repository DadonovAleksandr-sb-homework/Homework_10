<Window x:Class="Homework_10.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Homework_10"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="70*"></RowDefinition>
            <RowDefinition Height="30*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"></ColumnDefinition>
            <ColumnDefinition Width="90*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
    
        <ListBox x:Name="UserList"
                 Grid.Column="0"
                 Grid.RowSpan="2"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 Margin="5"
                 >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" Margin="5" BorderThickness="1" CornerRadius="10">
                        <StackPanel Margin="10">
                            <TextBlock Text="{Binding Path=Name}" Margin="0" FontSize="15"/>
                            <TextBlock Text="{Binding Path=Msg}" Margin="0" FontSize="20"/>
                            <TextBlock Text="{Binding Path=Time}" Margin="10" FontSize="10" Foreground="Gray" TextAlignment="Right"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <ListBox x:Name="LogList"
                Grid.Column="1"
                Grid.Row="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="5">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" Margin="5" BorderThickness="1" CornerRadius="10">
                        <StackPanel Margin="10">
                            <TextBlock Text="{Binding Path=Name}" Margin="0" FontSize="15"/>
                            <TextBlock Text="{Binding Path=Msg}" Margin="0" FontSize="20"/>
                            <TextBlock Text="{Binding Path=Time}" Margin="10" FontSize="10" Foreground="Gray" TextAlignment="Right"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GroupBox Grid.Column="1" 
                  Grid.Row="1" 
                  FontSize="20" 
                  Header="Отправка сообщения" 
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="5">
            <StackPanel>
                <TextBlock x:Name="TargetSend"
                           FontSize="8"
                           Text="{Binding ElementName=UserList, Path=SelectedItem.Id}"
                           Visibility="Visible"
                           Margin="5,2"/>
                <TextBox x:Name="txtMsgSend" 
                         FontSize="20" 
                         Margin="5,2"/>
                <Button x:Name="btnMsgSend" 
                        FontSize="20" 
                        Content="Отправить" 
                        Margin="5,2"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
